// Generated by LiveScript 1.2.0
(function(){
  var ref$, flatten, take, unique, imagesIn, inThisCategory, shuffleArray, slugify, platformTypes;
  ref$ = require('prelude-ls'), flatten = ref$.flatten, take = ref$.take, unique = ref$.unique;
  ref$ = require('../functions/sort.ls'), imagesIn = ref$.imagesIn, inThisCategory = ref$.inThisCategory, shuffleArray = ref$.shuffleArray;
  slugify = require('../functions/slugify.ls').slugify;
  platformTypes = function(db){
    var types, i$, len$, type, j$, ref$, len1$, category;
    types = [
      {
        name: 'Mobile',
        categories: [
          {
            name: 'Android',
            fa: 'fa-android'
          }, {
            name: 'iOS',
            fa: 'fa-mobile'
          }
        ]
      }, {
        name: 'Computer',
        categories: [
          {
            name: 'BSD',
            fa: 'fa-lock'
          }, {
            name: 'GNU/Linux',
            fa: 'fa-linux'
          }, {
            name: 'OS X',
            fa: 'fa-apple'
          }, {
            name: 'Windows',
            fa: 'fa-windows'
          }
        ]
      }, {
        name: 'Network',
        categories: [
          {
            name: 'Routers',
            fa: 'fa-sitemap'
          }, {
            name: 'Servers',
            fa: 'fa-tasks'
          }
        ]
      }
    ];
    for (i$ = 0, len$ = types.length; i$ < len$; ++i$) {
      type = types[i$];
      for (j$ = 0, len1$ = (ref$ = type.categories).length; j$ < len1$; ++j$) {
        category = ref$[j$];
        category.slug = slugify(category.name);
      }
    }
    return types;
  };
  exports.platformTypes = platformTypes;
}).call(this);
