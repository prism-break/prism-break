mixin subcategory-top(data)
  .card.mega
    .btn-rectangle.mega
      .category
        a(href="../../categories/#{data.category.slug}")
          = data.category.name
        = ' /'
        //!= ' &raquo; ' + data.subcategory.name
      .title= data.subcategory.name
