p#notice = notice

- @category.softwares.order(:title).each do |software|
  = render 'softwares/card', software: software


- if @category.parent == nil
  = link_to t('all'), category_all_path, class: 'bar right'

- @category.children.each do |subcategory|
  = link_to subcategory.title, subcategory, class: 'bar right'

br
= link_to t('v.categories.edit'), edit_category_path(@category)
|  &middot;  
= link_to '[x]', @category, :confirm => 'Are you sure?', :method => :delete
