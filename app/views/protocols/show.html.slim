p#notice = notice

= render 'protocols/card', protocol: @protocol

.text-block= filter_markdown(@protocol.description)

- if @protocol.softwares == []
  .pipe= t 'heading.no-supported-software'
- else
  .pipe
    = @protocol.title
    '
    = t 'heading.supported-software'
  - @protocol.softwares.each do |software|
    = render 'softwares/card', software: software

= link_to edit_protocol_path(@protocol), class: 'bar' do
  .label= t('v.protocols.edit')
  .icon= fa_icon 'edit'
